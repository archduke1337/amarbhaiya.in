/**
 * @fileoverview Moderator dashboard — reports summary, flagged content count.
 */
export default function ModeratorDashboardPage() {
  return (
    <div>
      <h1 className="text-3xl font-bold mb-6">Moderator Dashboard</h1>
      <p className="text-muted-foreground mb-8">Review reports and manage community health.</p>
      {/* TODO: FlaggedContentCount, RecentActions, ReportsSummary */}
    </div>
  )
}
